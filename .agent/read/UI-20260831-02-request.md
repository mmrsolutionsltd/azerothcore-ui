REQUEST UI-20260831-02
Goal: Make revive a prominent, centered action on each hero header card and remove the duplicate Revive tool from the right-hand Player Actions sidebar.
Scope: AzerothCore-UI.Web shared hero-card/header components and PlayerActions UI; tests as needed. No API, server, database, or client changes.
Current evidence: HeroCard already renders a small Revive action alongside card controls; PlayerActionTools still exposes a separate revive tool.
Constraints: Preserve existing players.services authorization, disabled/alive/offline behaviour, multi-selection semantics, and the current revive API call. Keep the high-fidelity WoW-inspired visual language and make the action especially clear for dead characters in dungeons.
Expected result: Revive is visually centered/prominent in the hero card (with clear dead/available state), the sidebar revive tool is removed, no unrelated tools regress, and relevant Web tests pass.
