RESPONSE UI-20260831-02
Status: complete
Changed: HeroCard.razor(.css), PlayerActionTools.razor, PlayerActionsSidebar.razor, Web.Tests
Actions: Replaced the small per-card Revive button with a full-width centered revive-banner button placed directly under the health track (before stats/actions), styled muted/disabled when alive, muted for offline (status unknown), and a pulsing red/gold "Revive!" banner when dead. Added a HideRevive parameter to PlayerActionTools (default false) so the sidebar can opt out via PlayerActionsSidebar while other consumers (e.g. CharacterDetails.razor) keep their Revive tool unaffected. Also fixed two pre-existing test breaks from the earlier layout-reorder commit (f19e8f5) that hadn't been caught: ToolCollectionComposesAllReusableActionPanels' expected heading order, and a stale .revive-button selector.
Verification: `dotnet build AzerothCore-UI.slnx` clean; `dotnet test AzerothCore-UI.Web.Tests` 77 passed; `dotnet test AzerothCore-UI.Api.Tests` 208 passed.
Rollback: Revert the listed Web component/CSS/test changes; no database, server, or client files were changed.
Next: none.
