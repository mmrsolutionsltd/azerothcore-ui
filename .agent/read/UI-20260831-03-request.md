REQUEST UI-20260831-03
Goal: Make the header roster expose a clear single active/focused hero in addition to multi-selected action targets, and make dependent pages refresh when that active hero changes.
Scope: AzerothCore-UI.Web shared RealmRosterHeader/HeroCard/selection store and header-aware pages (especially Artisan Gearing Room); Web.Tests. No API, server, database, or client changes.
Current evidence: Hero cards already distinguish target membership, and the shared SelectedCharacterStore has an active character concept, but the active state is not sufficiently prominent and dependent pages can retain stale data after a header hero click.
Constraints: Preserve multi-target selection, leader-first ordering, right-click/select-only behaviour, companion controls, panel tabs, and existing authorization. Keep exactly one active hero; clicking Focus or an eligible hero updates it without clearing targets. Avoid unnecessary polling/API calls; use the existing selection-change notification path.
Expected result: Active hero has an obvious WoW-styled visual treatment distinct from TARGET, all relevant pages subscribe/react to active-selection changes and refresh their hero-specific data (including Artisan Gearing Room), and focused Web tests cover active-vs-target state and automatic refresh.

Gearing-room refinement: Keep the attractive character-info styling, colours, headers, and labels, but remove the unused central paper-doll/sigil area (including crossed-swords decoration). Show only equipment slots that have upgrade paths, in one compact vertical list on the left, with the corresponding upgrade recommendations flowing in the main/right area. Preserve the WoW-inspired visual treatment and responsive layout.
