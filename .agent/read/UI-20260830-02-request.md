REQUEST UI-20260830-02
Goal: Redesign the shared header character cards into taller, thin traditional playing-card proportions and embed each character's Artisan Gearing/crafting upgrade view inside its card behind a compact switch or expandable control.
Scope: AzerothCore-UI.Web shared roster/header components, Artisan Gearing Room components/styles, tests; no database, no AzerothCore server changes, no restart.
Current evidence: Shared header is RealmRosterHeader.razor/.css and is used across compatible pages. Artisan planner is CraftingUpgradePlanner.razor/.css. Existing selected cards already show WoW-inspired class colours, health/status, leader state, and companion controls. Owner wants the header cards longer/thinner, with upgrades available per character without leaving the header.
Constraints: Preserve multi-select, leader-first ordering, online/offline/bot filters, account scoping, selected state, responsive wrapping, and existing permissions. Do not duplicate character-selection implementations. Keep the default card compact; upgrades should be opt-in via a switch/expand control so the header does not become permanently huge. Reuse existing crafting data/services and avoid loading all upgrade detail until requested if practical.
Expected result: A visually polished, responsive playing-card-style header roster. Each selected character card has a clear compact control (for example, Upgrades toggle/details) that reveals that character's crafting upgrade information and stat/source details using the existing Artisan visual language. Add/update component tests and report build/test results.
