RESPONSE UI-20260830-02
Status: complete
Changed:
- AzerothCore-UI.Api/Services/ArmorProficiencyRules.cs (new, shared, corrected armor/weapon
  proficiency rules - fixed a real bug found while scoping this: level-40 Classic-era gating
  no longer applies on WotLK 3.3.5a, and only each class's primary armor type is now
  recommended). CraftingUpgradeRules, DungeonGuideService, and ServerAdministrationController
  all now call the one shared implementation instead of three duplicated, buggy copies.
- AzerothCore-UI.Web/Components/Shared/Crafting/{GearSlotButton,UpgradeCard,UpgradeDrawer}.razor(.css)
  (new) + AzerothCore-UI.Web/Models/CraftingVisuals.cs (new): extracted from
  CraftingUpgradePlanner.razor so the full Artisan Gearing Room page and the new roster
  embed share identical markup/CSS.
- AzerothCore-UI.Web/Components/Shared/RealmRosterHeader.razor(.css): each selected hero
  card gets an "Upgrades" toggle (policy players.characters) that lazily fetches
  GetCraftingUpgradePlanAsync on first open only, caches per character for the session, and
  renders KPI counts + gear slots + the upgrade drawer in a popover anchored to the row
  (not the individual card, which clips via overflow:hidden) so it never resizes sibling
  cards or the sticky header. Never called from the 15s roster refresh loop. Added a
  narrow-viewport (<800px) fixed bottom-sheet fallback. Added 1500px/3200px breakpoints
  that scale up only the selected-hero cards (not the candidate hero-choice strip), per
  owner request - collapsed/default card look is otherwise unchanged.
- Also, per direct owner follow-up in the same session: PlayerActionTools grid restyled
  to a permanent right-hand sidebar (AzerothCore-UI.Web/Components/Shared/PlayerActionsSidebar.razor,
  embedded in MainLayout.razor), collapsible below 1200px; PlayerActionTools gained a
  SingleColumn parameter; PlayerActionToolHeader simplified to a centered icon+title strip
  with a new PlayerActionToolFooter carrying the result message and bottom-right action
  buttons across all 8 tools; found and fixed a pre-existing layout bug where
  .player-action-tool was flex-row instead of flex-column.
Verification: dotnet build (full solution) clean; AzerothCore-UI.Api.Tests 208/208 passed;
AzerothCore-UI.Web.Tests 68/68 passed (11 new tests added for the roster upgrades embed,
the extracted Artisan components, and the sidebar).
Rollback: standard UI release rollback (previous /opt/azerothcore/admin/releases/* symlink
swap); no database changes were made.
Next: none from this exchange. One deliberate deviation from your original request worth
noting - RealmRosterHeader.razor.css already used position:relative + overflow:hidden on
.online-hero-card for its class-color accent bar, so the upgrade panel is anchored to
.hero-card-row (the row) rather than the individual card to avoid being clipped; also kept
the standalone /player-actions page/nav entry rather than retiring it, since it has
dedicated bUnit coverage (PlayerActionToolTests.PlayerActionsUsesTheSharedHeaderTargets...)
that's still valuable as a fallback surface alongside the new always-on sidebar.
