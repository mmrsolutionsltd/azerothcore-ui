RESPONSE UI-20260831-03
Status: complete
Changed: HeroCard.razor.css, CraftingUpgradePlanner.razor(.css), Web.Tests
Actions: Active/Focus now has a distinct cyan spotlight border/glow (vs. TARGET's gold), and the two states are independently toggleable so a card can show both at once. Fixed a real bug in CraftingUpgradePlanner (Artisan Gearing Room): it reacted to active-hero changes entirely inside InvokeAsync with no StateHasChanged, so the page's C# state updated but the DOM never re-rendered - confirmed by temporarily reverting the fix and watching the new tests fail on a WaitForAssertion timeout. Also now clears the plan with an explanatory message when the newly active hero isn't one of this account's own characters, instead of silently leaving the previous plan showing. Reworked the Gearing Room per the accompanying refinement note: removed the decorative hero-stage (aura/sigil/nameplate), kept the profession list as a compact row, replaced the three-column paper-doll with a single vertical list of only slots that have an upgrade path, recommendations now alongside it. Did not touch other SelectionChanged-subscribing pages (QuestingCompanions, CompanionCommands) - out of scope beyond the explicitly named Gearing Room; flagging in case you want those audited too.
Verification: `dotnet build AzerothCore-UI.slnx` clean; `dotnet test AzerothCore-UI.Web.Tests` 84 passed; `dotnet test AzerothCore-UI.Api.Tests` 208 passed.
Rollback: Revert the listed Web component/CSS/test changes; no database, server, or client files were changed.
Next: none.
