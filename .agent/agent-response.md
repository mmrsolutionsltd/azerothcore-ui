RESPONSE SERVER-20260831-01
Status: needs-owner-input
Changed: azerothmedia only - new modules/mod-hunter-pack-companion/ (src/HunterPackCompanion.h, HunterPackCompanion.cpp, HunterPackCompanion_Scripts.cpp, hunterpackcompanion_loader.cpp, conf/mod_hunterpackcompanion.conf.dist, README.md) plus a CMake reconfigure (cmake . in /opt/azerothcore/build) to register it. Nothing else touched; no database changes; no installed binary changed; no service restarted.
Actions: Implemented per the approved design - PlayerScript hooks OnPlayerAfterGuardianInitStatsForLevel (remembers the real hunter pet's creature entry, syncs pack size), OnPlayerLevelChanged (resyncs pack size), OnPlayerAddToBattleground/OnPlayerUpdateArea+map checks/OnPlayerBeforeTeleport/OnPlayerJustDied (despawn extras), OnPlayerLogout (forgets all state for that player). Extras are TempSummon (manual despawn) cloned from the real pet's entry, explicitly SetOwnerGUID'd to the player so the core's existing charmer-or-owner resolution routes kill credit/quest credit/loot to the owner - confirmed this by reading Unit::GetOwnerGUID()/TempSummon's separate summoner-GUID tracking before writing the spawn code, since a plain TempSummon does NOT get that routing for free. Config matches the .conf.dist proposed earlier (Enable/LevelInterval/MaxPets/SuppressInBattleground/SuppressInInstance), loaded via a WorldScript::OnAfterConfigLoad hook. Did not wire into src/test's unit_tests target - the ACORE_MODULE_TEST_SOURCES CMake property that would need is read by src/test/CMakeLists.txt but nothing in this codebase actually sets it anywhere (checked before committing to that plan), so inventing that wiring blind seemed like a bigger risk than being upfront that it's untested; the README's manual QA checklist stands in for it as originally flagged.
Verification: Reconfigured (cmake .) - the new module and its .conf.dist were correctly discovered and listed. Built in isolation: `cmake --build /opt/azerothcore/build --target worldserver -j2`, EXIT_CODE=0, [1722/1722] linked cleanly, zero warnings from the new module's own files (only pre-existing warnings elsewhere in mod-playerbots and one unrelated system-header notice). Built binary sits at /opt/azerothcore/build/src/server/apps/worldserver, not installed.
Rollback: Nothing installed, so nothing to roll back yet. If you want to back out the source change instead: rm -rf modules/mod-hunter-pack-companion in /opt/azerothcore/source/core, then cmake . again to drop it from the build.
Next: Owner's own install/verify boundary from their approval - do not install, restart, or deploy without another explicit go-ahead. If approved to install: docs/handover/06-operations-cookbook.md's existing steps (backup current worldserver binary, stop azerothcore-world.service, copy the new binary in, start the service), then walk the README's manual QA checklist (level thresholds, single loot roll, instance/BG suppression, death/logout/dismiss-resummon cleanup) before calling it done.
