RESPONSE SERVER-20260831-01
Status: needs-owner-input
Changed: azerothmedia only. Rewrote modules/mod-hunter-pack-companion/src/HunterPackCompanion.{h,cpp} to fix the reported live defect, and reinstalled. No database changes.
Actions: Root-caused the "horse-sized, green/friendly, doesn't attack" defect by reading the actual AC summon/AI pipeline rather than guessing:
1) The spawn call passed no SummonPropertiesEntry, so Map::SummonCreature's mask-selection defaulted to UNIT_MASK_SUMMON and constructed a plain TempSummon, not even a Guardian - the class the design promised never actually got used.
2) Scale: TempSummon uses the raw creature_template's native scale, which differs from a real Pet's actual displayed scale for many tameable beast entries (source of "horse-sized").
3) The actual reason it wouldn't fight: PetAI::UpdateAI() bails out immediately if GetCharmInfo() is null (read the source directly), and nothing about being a Guardian gives a summon CharmInfo for free - only the charm-state-driven AI swap in Unit::UpdateCharmAI() does that, which nothing here was triggering. Even a proper Guardian doesn't get PetAI just from its class: CreatureAISelector::SelectAI only forces PetAI for creature->IsPet() (the literal stable-able Pet class, a different unit-mask than Guardian) or otherwise falls back to whatever the creature_template's DB-configured AIName happens to be for that cloned pet entry - not something this module controls.
Fix: pass SummonProperties.dbc entry 67 (the same properties AC's own EffectSummonPet uses for its non-player-owner guardian-pet fallback) so the clone is a real Guardian; copy the real pet's live GetObjectScale() (not the template's native scale) onto each clone; explicitly force the AI with summon->AIM_Initialize(new PetAI(summon)) after summon->InitCharmInfo() and SetReactState(REACT_AGGRESSIVE), rather than trusting the cloned template's AIName column. Verified AIM_Initialize takes ownership of the passed AI safely before relying on it (read Creature::AIM_Initialize's implementation).
Noted your visual-refinement suggestion (optional mount-sized cosmetic pack mode, kept separate from combat readiness) - not implemented, flagged as a future enhancement once the base combat behaviour is confirmed working; didn't want to add scope before the blocking defect is actually resolved.
Verification: isolated rebuild - EXIT_CODE=0, only 5 build steps this time (incremental, no reconfigure needed), zero errors. Backed up the (defective) binary that was live as worldserver.pre-hunterpack-fix-<timestamp>, confirmed no players connected, installed the fix, restarted azerothcore-world - clean boot, zero errors in the log, all 449 bots logged in normally.
Rollback: two backups now available if needed - worldserver.pre-hunterpack-20260831-151502 (pre-module, original), worldserver.pre-hunterpack-fix-<timestamp> (module installed but with the reported defect). Current live binary is the fixed version.
Next: needs another round of live QA against the specific reported defect - outdoor hostile engagement (does it actually attack), correct size, correct reaction/nameplate behaviour - plus the rest of the original checklist (XP, quest credit, loot, threat, instance/BG suppression, dismiss/resummon/death/revive cleanup) since none of that could be meaningfully assessed while the pack wasn't fighting at all.
